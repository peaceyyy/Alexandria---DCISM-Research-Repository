phase: admin-dashboard
created: 2026-07-01
schema: athena-default
feature: members-management

dependencies:
  read:
    - path: docs/DESIGN.md
      reason: Follow guidelines for table row actions (hover states, restrained color usage).
      priority: critical
    - path: features/admin/components/data-table.tsx
      reason: Reuse the table component built in the dashboard sub-phase.
      priority: high
    - path: features/admin/lib/mock-data.ts
      reason: Extend mock data to include members.
      priority: high
      
  modify:
    - path: features/admin/lib/mock-data.ts
      reason: Add mock members data to the existing file.
      exclusive_lock: true
      
  create:
    - path: app/(admin)/members/page.tsx
      reason: Route entry point for the members management page.
      exclusive_lock: true

relationships:
  requires_phases:
    - 01-admin-layout-shell
    - 02-dashboard-metrics
  provides_for_phases: []

context_estimate:
  read_tokens: 1000
  total_budget: 5000
  warning_threshold: 4000
  status: ok
