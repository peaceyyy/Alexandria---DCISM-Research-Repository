phase: admin-dashboard
created: 2026-07-01
schema: athena-default
feature: admin-layout-shell

dependencies:
  read:
    - path: docs/DESIGN.md
      reason: Understand color palette, typography (Khula/Inter), and layout grid intent.
      priority: critical
    - path: docs/PRODUCT.md
      reason: Understand product register and dashboard requirements.
      priority: high
      
  modify:
    - path: app/globals.css
      reason: Ensure CSS variables support the admin layout (if any new tokens are needed, though mostly relying on existing).
      exclusive_lock: false
      
  create:
    - path: app/(admin)/layout.tsx
      reason: Setup the Next.js route group layout for all admin pages.
      exclusive_lock: true
    - path: features/admin/components/admin-sidebar.tsx
      reason: Left navigation component.
      exclusive_lock: true
    - path: features/admin/components/admin-shell.module.css
      reason: Shell grid layout styles.
      exclusive_lock: true

relationships:
  requires_phases: []
  provides_for_phases:
    - 02-dashboard-metrics
    - 03-members-management

context_estimate:
  read_tokens: 1500
  total_budget: 5000
  warning_threshold: 4000
  status: ok
