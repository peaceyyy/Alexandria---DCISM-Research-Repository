phase: admin-dashboard
created: 2026-07-01
schema: athena-default
feature: dashboard-metrics

dependencies:
  read:
    - path: docs/DESIGN.md
      reason: Understand visual guidelines for metric cards and typography.
      priority: critical
    - path: features/admin/components/admin-shell.module.css
      reason: Ensure new components fit properly within the established shell.
      priority: high
      
  modify: []
      
  create:
    - path: app/(admin)/dashboard/page.tsx
      reason: Route entry point for the dashboard.
      exclusive_lock: true
    - path: features/admin/components/stat-card.tsx
      reason: Reusable metric card UI.
      exclusive_lock: true
    - path: features/admin/components/data-table.tsx
      reason: Reusable table shell for dashboard and members.
      exclusive_lock: true
    - path: features/admin/lib/mock-data.ts
      reason: Mock data source for the tables before backend binding.
      exclusive_lock: true

relationships:
  requires_phases:
    - 01-admin-layout-shell
  provides_for_phases: []

context_estimate:
  read_tokens: 1200
  total_budget: 5000
  warning_threshold: 4000
  status: ok
